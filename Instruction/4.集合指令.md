# 集合指令

支持可迭代集合（切片或字典）的一些基本操作。


## 定义

指令码区间：`[35-45]`，11个。

```go
码值    指令字      说明
-----------------------------------------------------------
35      SLICE(1)    子切片
        // 从目标切片创建一个子切片。
        // 注意是在原切片上截取引用，创建副本应使用COPY。
        // 附参：子切片大小（<256），0表示目标位置后全部。
        // 实参1：目标切片。
        // 实参2：起始下标（从0开始），支持负数从末尾算起。
        // 返回值：一个子切片。
        // 例：
        // 假设目标集：[1, 2, 3, 4, 5, 6, 7, 8]
        //      {2} SLICE[3]    // [3, 4, 5]
        //      {-4} SLICE[2]   // [5, 6]
        //      {-3} SLICE      // [6, 7, 8]
        //      {8} SLICE       // [] 空切片
        // 注意：
        // 指令遵循严格模式，如果位置或数量溢出，执行会失败。

36      REVERSE     反转（切片）
        // 将切片内的成员反转排列。
        // 实参：目标切片。
        // 返回值：反转后的新切片。
        // 例：
        // {1, 2, 3, 4, 5}
        // REVERSE  // [5, 4, 3, 2, 1]

37      MERGE       合并（切片）
        // 将多个切片合并为一个切片。
        // 实参：不定数量。
        // 返回值：一个新切片。
        // 例：
        // {1, 2, 3}
        // {10, 20}
        // @POP @POP MERGE  // [10, 20, 1, 2, 3]

38      EXTEND      扩充（切片）
        // 向目标切片内添加成员。
        // 实参：不定数量，首个为目标集，其余为待添加成员。
        // 返回值： 一个新切片。
        // 例：
        // {1, 2, 3}
        // {10}
        // {20}
        // @POPS[3] EXTEND  // [1, 2, 3, 10, 20]

39      PACK        封装（压实打包）
        // 将切片成员解码串接在一起。
        // 压实打包，成员之间底层数据紧密集成。
        // 实参：一个切片。
        // 返回值：一个字节序列（新值）。
        // 例：
        // SHIFT[3] PACK 将栈顶3项数据解码打包为一体。
        // 注：
        // 如果成员是 Any，每项都会解码到其基础二进制形态并追加。

40      SPREAD      展开（切片）
        // 将目标切片展开，成员提升。
        // 如果前置取值指令，会展开添加到目标空间。
        // 实参：一个切片。
        // 返回值：不定数量成员。
        // 例：
        // $SPREAD  将目标集展开添加到当前局部域。
        // @SPREAD  将目标集展开添加到实参区。
        // SPREAD   将栈顶项展开后逐个入栈（维持原顺序）。
        // 提示：
        // 连续的 @~SPREAD 可将栈内多个切片逐一展开到实参区。

41      INDEX       成员（切片）
        // 获取目标切片的成员或成员集。
        // 实参1：目标切片。
        // 实参2：位置下标或下标集。
        // 返回值：值或值集。
        // 说明：
        // 切片的数值下标支持负数从末尾算起。
        // 如果位置是一个下标集，则返回一个值集。
        // 例：
        // {1} INDEX    取切片中的第二个成员
        // {1, 3} INDEX 取切片中第二和第四个成员。
        //
        // 设计：
        // 主要用于取切片中单个值或不连续位置的值。
        // 没有对切片成员赋值的指令，因为那可能改变脚本本身。

42      ITEM        条目（字典|对象|模块）
        // 获取集合中的成员或成员集。
        // 适用：取字典、对象、模块中的静态成员。
        // 实参1：目标集合。
        // 实参2：成员键或键集。
        // 返回值：值或值集。
        // 说明：
        // 如果键名对应的条目不存在，会取到一个默认零值（nil）。
        // 如果键名是一个集合，则返回一个值集。
        // 例：
        // "AAA" ITEM           获取目标集内 AAA 对应的值。
        // {"AA", "BB"} ITEM    获取目标集内 AA 和 BB 的值。

43      SET         设置（字典）
        // 设置字典（插入序 map[string]any）的键值对应。
        // 实参1：目标字典。
        // 实参2：键名或键名集（[]string）。
        // 实参3：目标值或值集。
        // 返回值：原目标字典。
        // 说明：
        // 如果键为一个字符串（键名），则值任意。
        // 如果键为一个清单，同时值也是一个清单，则一一对应赋值，
        // 否则同一个值对应全部键名。
        // 注意：如果键和值都为集合，两者长度需一致。
        //
        // 设计：
        // 字典并不是栈脚本中的主要集合类型，
        // 但在转换指令系中，支持由两个切片创建字典，以提供某些方便。
        //
        // 安全性：
        // 字典是一个运行时对象，其内容由实参创建。
        // 修改字典并不会改变实参本身，因此不涉及脚本内容只读的规范。

44      CALL(1)     调用（对象|模块）
        // 对目标集合中的方法执行调用。
        // 附参：传递给方法的实参数量。
        // 实参1：目标集合。
        // 实参2：方法名（大小写敏感）。
        // 实参...：方法需要的实参序列。
        // 返回值：目标方法调用的结果。
        // 如果方法返回多个值，会被封装为一个切片（单值）。
        // 注记：
        // 即便方法定义中是不固定参数，也没有影响。
        // 因为这是运行时指示解析器从数据栈/实参区中取出数据的条目数，
        // 属于调用逻辑而非定义配置。
        // 例：
        // DATE{2026-03-21T19:08:50Z}
        // TOP "Day"            // TOP 引用栈顶，不弹出维持后用
        // @CALL                //=> 21 暂存
        // TOP "Clock"          // TOP 引用不弹出
        // @CALL                //=> [19, 8, 50] 暂存
        // "Format" "15_04_05"  // 不再引用 DATE，会弹出作为首个实参
        // @CALL[1]             //=> "19_08_50" 暂存
        // PUSH                 // [21, [19, 8, 50], "19_08_50"] 入栈
        //
        // 例：
        // MO_MATH
        // TOP "Abs" {-10}      // TOP 仅引用 MO_MATH
        // @CALL[1]             //=> 10.0  暂存实参区
        // "Pow" {2} {8}        // 栈顶依然为 MO_MATH，将被取参弹出
        // @CALL[2]             //=> 256.0  暂存实参区
        // PUSH                 // [10.0, 256.0]  实参区两项压入栈
        // SHIFT[2]             // 栈顶2项打包为切片
        // ANYS{Int}            //=> [10, 256]  转换为整数序列
        //
        // 例：
        // MO_FMT               // fmt 模块
        // "Sprint"             // fmt.Sprint(a ...any) 不定参数
        // DATE{1606227845000}  // 一个Time
        // CALL[1]              // 实际向 Sprint 传递一个实参
        //
        // 设计：
        // 调用指令是通用的，可以解耦模块中方法的直接定义，
        // 同时这也适用于对象，如内置的正则表达式和时间对象。
        //
        // 改进效果：
        // 与原有设计比，这使得模块的方法不需要硬编码参数数量，
        // 同时，模块的方法数量也没有了限制。
        //
        // 模块只需简单创建（无需附参），其安全性由实现本身保证。

45      SIZE        大小（切片|字典）
        // 获取集合的大小。
        // 实参：一个集合。
        // 返回值：一个整数。
```
